#include <bits/stdc++.h>

using namespace std;
int main()
{
    int M, N;
    float a, b;
    cin >> M >> N;

    a = M * 1.00 / 2;
    b = N * 1.00;

    cout << floor(a * b);

    return 0;
}