#include <bits/stdc++.h>

using namespace std;

int main()
{
    int64_t N;
    long long K;

    cin >> N >> K;

    long long A[N];

    for (long long i = 0; i < N; i++)
    {
        scanf("%d", &A[i]);
    }
}